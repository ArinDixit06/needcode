# OpenRouter API Key (Optional if you input it in the UI directly)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Required to protect the website. Set this only in the server environment.
WEBSITE_PASSWORD=replace_with_a_strong_password

# Port for backend server
PORT=5000
